import { Libre_Franklin, Rokkitt } from "next/font/google";

export const franklin = Libre_Franklin({
  subsets: ["latin"],
  display: "swap",
});

export const rokkitt = Rokkitt({
  subsets: ["latin"],
  display: "swap",
});
